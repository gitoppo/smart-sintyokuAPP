# GAS_CHANGELOG.md

`factory_progress_gas_full.gs` は、出荷管理アプリ（事務アプリ）・進捗管理アプリ（現場アプリ）の
**両方から共有されているGASバックエンド**です。

どちらのチャットで編集する場合も、作業を始める前に必ずこのファイルを確認し、
自分が把握していない変更が入っていないかチェックしてください。

## なぜこのファイルが必要か

過去に、片方のチャットで加えた修正（重複防止ロジック）がGitHubへpushされないまま
Apps Scriptにのみデプロイされていたため、もう片方のチャットが別件の修正時に
GitHubから取得した古いコードを土台にデプロイしてしまい、修正が消える事故が発生しました。
このファイルと下記ルールは、その再発防止のためのものです。

## 編集時のルール

1. **編集を始める前に、GitHubから最新の`factory_progress_gas_full.gs`を取得する**
   （そのチャットに以前アップロードされた古いローカルコピーを土台にしない）
2. ファイル冒頭の「内部バージョン」表記が、自分の知っている最新と一致しているか確認する。
   一致しない場合は、先に最新版を取り直してから編集を始める
3. 編集後は、以下の**両方**を必ず行う
   - GitHubへのpush
   - Google Apps Scriptエディタへの貼り付け＋「新しいバージョン」でのデプロイ
   （片方だけでは、もう一方のアプリ・チャットに反映されません）
4. 編集内容を、このファイルの「変更履歴」に追記する（日付・担当チャット・対象関数・内容を数行で）
5. `.gs`ファイル冒頭の「内部バージョン」番号を1つ進める

## 変更履歴

### v1 - 2026年8月18日（事務アプリチャット・進捗アプリチャット 合同で復旧・統一）

**経緯**：事務側チャットで追加した`saveMaterialMovement_`の重複防止ロジックが、
GitHubへpushされないままApps Scriptにのみデプロイされていた。その後、進捗側チャットが
別件（ストック機能）修正のためGitHubから取得した（重複防止ロジックを含まない）古いコードを
土台に編集・デプロイしたため、重複防止ロジックが失われる事故が発生。
両チャットの変更内容を統合し、このバージョンとして復旧・統一した。

- `saveMaterialMovement_`：同じidのデータが既に存在する場合は追加しない（入荷・返品の重複登録防止）
  ［事務アプリチャット］
- `saveStock_`：削除フラグ（`isDelete`・`deletedIds`）対応を追加。削除操作時に該当itemIdを
  クラウドから明示的に削除してからマージ保存するよう変更 ［進捗アプリチャット］
- `readStockSheet_`：B列が数値（旧形式）の場合に空オブジェクトとして扱うよう対応
  ［進捗アプリチャット］

---

### v2 - 2026年8月26日（事務アプリチャットで対応）

**経緯**：v1の直後、`.gs`ファイル冒頭に「内部バージョン」表記コメントを追加していたが、
その後の別件対応（材料在庫の重複復活バグ調査）で、進捗アプリチャット側が作成した統合版`.gs`に
差し替えた際、このコメントブロックが引き継がれずに失われていたことが判明。今回、コメントを復元。

- `archiveMaterialMovements_`：アーカイブの保存先を、本体スプレッドシート→**外部の保管庫スプレッドシート**へ変更。
  （新設時、本体シートに書き込む実装になっており、数年運用すると本体シートが肥大化する設計ミスがあったため修正）
  ［事務アプリチャット］
- `archiveDeliveryHistory_`：新設。出荷履歴（`deliveryHistory`）を外部保管庫へアーカイブする機能。
  `deliveryHistory`にはこれまでアーカイブの仕組みが無かった。他のアーカイブ対象と異なり複数列形式の
  シートのため、`readJsonSheet_`/`writeJsonSheet_`ではなく行・列を直接操作する専用実装
  ［事務アプリチャット］
- `migrateOperationLogArchive_`（1回限りの移行機能）の移行対象に、`materialMovements_archive`・
  `deliveryHistory_archive`を追加（既に本体シートに残っている分があれば、これで保管庫へ回収できる）
  ［事務アプリチャット］

---

（以降、`.gs`を編集するたびに、ここへ追記してください）
